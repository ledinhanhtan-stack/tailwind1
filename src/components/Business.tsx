import React, { FunctionComponent } from 'react';

interface BusinessProps {
}

const Business: FunctionComponent<BusinessProps> = (props) => {
  
  return (
    <div></div>
  )
};

export default Business;
