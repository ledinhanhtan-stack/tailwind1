import React, { FunctionComponent } from 'react';

interface FeedbackCardProps {
}

const FeedbackCard: FunctionComponent<FeedbackCardProps> = (props) => {
  

  return (
    <div></div>
  )
};

export default FeedbackCard;
