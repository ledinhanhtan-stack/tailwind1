import React, { FunctionComponent } from 'react';

interface ClientsProps {
}

const Clients: FunctionComponent<ClientsProps> = (props) => {
  
  return (
    <div></div>
  )
};

export default Clients;
