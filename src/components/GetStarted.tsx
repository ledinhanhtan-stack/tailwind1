import React, { FunctionComponent } from 'react';

interface GetStartedProps {
}

const GetStarted: FunctionComponent<GetStartedProps> = (props) => {
  
  return (
    <div></div>
  )
};

export default GetStarted;
